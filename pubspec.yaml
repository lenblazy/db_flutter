name: db_flutter
description: "A Database service package"
version: 1.2.0
homepage:
publish_to: none

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get_it:
  injectable:
  path: ^1.9.0
  sqflite:
  sqflite_common:
  sqflite_sqlcipher: ^3.2.1
  storage_flutter:
    path: ../../core/storage_flutter


dev_dependencies:
  build_runner: ^2.4.14
  flutter_lints:
  flutter_test:
    sdk: flutter
  injectable_generator:
  mocktail: ^1.0.4
  sqflite_common_ffi:

flutter:
